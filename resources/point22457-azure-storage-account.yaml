apiVersion: storage.azure.crossplane.io/v1alpha3
kind: Account
metadata:
  name: "point22457"
  labels:
    example: "true"
spec:
  resourceGroupName: "system:redbox"
  storageAccountSpec:
    kind: Storage
    location: "UK South"
    sku:
      name: Standard_LRS
      tier: Standard
    tags:
      application: crossplane
  providerConfigRef:
    name: default
  writeConnectionSecretToRef:
    namespace: crossplane-system

---


apiVersion: storage.azure.crossplane.io/v1alpha3
kind: Container
metadata:
  name: "point22457"
  labels:
    storage_account: "point22457"
spec:
  writeConnectionSecretToRef:
    name: "point22457-container-secret"
    namespace: crossplane-system
  # Azure containers read their credentials from an Account, not a Provider. We
  # use the providerRef field to specify which Account to read credentials from.
  providerConfigRef:
    name: "point22457"

